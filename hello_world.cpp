#include <iostream> // Input/Output stream
using namespace std; // Uses the std namespace (standard? I think)

int main() { // Main function
    cout << "Hello, World!"; // C output stream std which outputs "Hello, World!"
    return 0; // Returns 0 which is exit success
}
